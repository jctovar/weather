<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Disallow cleartext traffic globally -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

    <!-- Pin Open-Meteo certificate (optional, requires SHA-256 hashes) -->
    <!-- Enable after obtaining actual certificate pins from api.open-meteo.com -->
    <!--
    <domain-config>
        <domain includeSubdomains="true">api.open-meteo.com</domain>
        <pin-set expiration="2026-01-01">
            <pin digest="SHA-256">REPLACE_WITH_ACTUAL_PIN</pin>
        </pin-set>
    </domain-config>
    -->
</network-security-config>
